
<div class="container-fluid text-center">
  <div class="row content">
    <div class="col-sm-2 sidenav">
      <ul>
        {{#each burgers}}
        {{#unless devoured}}
          <li>
            <p>ID: {{id}}</p>
            <p>Burger: {{burger_name}}</p>
            <form action="/update?_method=PUT" method="POST">
              <input id="eatBurger" type="hidden" name="id" value="{{id}}">
              <button type="submit">Trash it</button>
            </form>
            <form action="/update?_method=PUT" method="POST">
              <input id="burger-input" type="hidden" name="id" value="{{id}}">
              <!-- <textarea name="burger" type="text" placeholder="update new burger"></textarea> -->
              <button type="submit">Devoute It!</button>
            </form>
          </li>
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-sm-8 text-left">
      <h1>data should be placed here</h1>
      <img src="/assets/img/burger.jpg" alt="">
      <form action="/" method="POST">
        <textarea id="burger-input" name="burger" type="text"></textarea>
        <button type="submit">Submit</button>
      </form>
      <hr>
      <h3></h3>
      <p>this works</p>
    </div>
    <div class="col-sm-2 sidenav">
      <div class="well">
        <p>ADS</p>
      </div>
      <div class="well">
        <p>ADS</p>
      </div>
    </div>
  </div>
</div>

<!-- <script type="text/javascript">
$('#eatBurger').on('click',function(){
  var burgers = [];
  var burger;
	event.preventDefault();
 	burger = $('#burger-input').val().trim();
	burgers.push(burger);
  console.log(burger);
  console.log(burgers);
	// makeButtons();
	$("#burger-input").val('');
});
</script> -->
